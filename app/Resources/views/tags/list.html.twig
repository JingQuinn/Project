{% extends '_base.html.twig' %}
{% block pageTitle %}list of tags{% endblock %}
{% block body %}
<ul>
    {% for tag in tags %}
        <li>
            id = {{ tag.id }}
            <br>
            name =
            <a href="{{ path('tags_show',{id:tag.id}) }}">{{ tag.name }}</a>
        </li>
    {% endfor %}
</ul>
    <hr>
    <li>
        <a href="{{ path('tags_new_form') }}">Create New Tag</a>
    </li>
    <li>
        <a href="{{ path('homepage') }}">back to home page</a>
    </li>
{% endblock %}