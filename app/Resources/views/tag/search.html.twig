{% extends '_base.html.twig' %}

{% block body %}
    <h1>Search</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Search" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('tag_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
